{
  "name": "cookgrim",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "npm run build -w @cookgrim/shared && concurrently -n api,web -c blue,magenta \"npm run dev -w @cookgrim/api\" \"npm run dev -w @cookgrim/web\"",
    "dev:api": "npm run dev -w @cookgrim/api",
    "dev:web": "npm run dev -w @cookgrim/web",
    "build": "npm run build -w @cookgrim/shared && npm run build -w @cookgrim/api && npm run build -w @cookgrim/web",
    "icons": "npm run icons -w @cookgrim/web"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
